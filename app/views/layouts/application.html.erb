<!DOCTYPE html>
<html>
<head>
  <title>Bartorville</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= yield(:name) if yield %>">

    <div class="container">
            <div class="row">
                <div class="span12"></div>
                    <div class="navbar navbar-inverse">
                        <div class="navbar-inner">
                            <a class="brand" href="#">Bartorville</a>
                            <ul class="nav">
                                <li class="active"><%= link_to "Home", root_path %></li>
                                
                                <li><%= dynamic_dashboard_links %></li>
                                <li><%= dynamic_login_links %></li>
                            </ul>
                        </div>
                    </div>
                </div>
            <div class="row">
                <div class="span3">
                    <%= yield :submenu%>
                    <%= render 'shared/search' %>
                </div>
                <div class="span9">
                    <%= yield %>
                </div>
            </div>
            <div class="row">
                <div class="span12"></div>
            </div>

</body>
</html>
